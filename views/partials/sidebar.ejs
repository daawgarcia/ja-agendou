<aside class="sidebar">
  <div class="sidebar-brand">
    <img src="/images/logo-ja-agendou.png" alt="Já Agendou!" class="logo-small" />
    <span class="brand-text">Já Agendou!</span>
  </div>

  <nav class="sidebar-nav">
    <a href="/dashboard" class="nav-link">Dashboard</a>
    <a href="/pacientes" class="nav-link">Pacientes</a>
    <a href="/agendamentos" class="nav-link">Agendamentos</a>
    <a href="/agendamentos/lembretes" class="nav-link">Lembretes WhatsApp</a>
        <% if (user && (user.perfil === 'super_admin' || user.perfil === 'admin')) { %>
          <a href="/dentistas" class="nav-link">Dentistas</a>
          <a href="/servicos" class="nav-link">Serviços</a>
        <% } %>
        <a href="/historico" class="nav-link">Histórico</a>
        <a href="/recibos" class="nav-link">Recibos</a>
        <% if (user && (user.perfil === 'super_admin' || user.perfil === 'admin')) { %>
          <a href="/relatorios" class="nav-link">Relatórios</a>
        <% } %>
    <% if (user && user.perfil === 'super_admin') { %>
      <a href="/clinicas" class="nav-link">Clínicas</a>
    <% } %>
  </nav>
</aside>
